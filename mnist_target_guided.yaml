seed: 0
device: cuda
dtype: float32
coupling: target_guided
num_regions: 16

data:
  root: datasets
  batch_size: 16
  n_points: 256
  num_workers: 0

model:
  point_dim: 2
  d_model: 256
  nhead: 8
  num_layers: 4
  dim_feedforward: 1024
  dropout: 0.0

training:
  num_steps: 50000
  learning_rate: 0.001
  weight_decay: 0.01
  log_every: 100

checkpoint: model_mnist_target_guided_16_sinusoidal.pt
