seed: 0
device: cuda
dtype: float32
coupling: independent

data:
  batch_size: 16
  n_points: 256

model:
  point_dim: 2
  d_model: 256
  nhead: 8
  num_layers: 4
  dim_feedforward: 1024
  dropout: 0.0

training:
  num_steps: 50000
  learning_rate: 0.0003
  weight_decay: 0.01
  log_every: 100

checkpoint: model_horse_independent.pt
